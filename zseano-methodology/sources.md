# sources.md — zseano-methodology

## Sources

- **Book**: *zseano's methodology* — Sean Roesner (zseano), PDF in the
  source repo. Practitioner methodology, ~2019–2020 era tooling.
- Current cross-references (2026 refresh): `recon-pipeline` (toolkit +
  attack-surface expansion runbook), `xss-cheat-sheet` + `payloads-all-the-things`
  (filter/payload depth), `web-security-academy` (modern classes),
  `owasp-api-security-top-10` (IDOR→BOLA/BOPLA naming), `hacking-the-cloud`
  (SSRF→metadata rules), `report-writing` (reporting norms).
- Methods-refresh source register: `docs/superpowers/2026-09-23-bug-bounty-source-register.md`
  (BugBounty workspace).

## Provenance & currency policy

- The *loop* (first look → expand surface → rinse/repeat) and the
  filter-first mindset are durable — keep as-is.
- Era-marked items get `> **…note:**` callouts: program/payout norms
  (ch01), named tools (ch02 → recon-pipeline), tactical ceilings
  (ch03/04), attack-surface volume/scope (ch06), automation cadence
  (ch07).
- All cross-account and delivery testing stays on researcher-owned
  accounts/surfaces; escalation chains are report narrative.

## Review history

- 2026-09-23: 7 chapter currency/ceiling callouts, cheatsheet/patterns
  notes, SKILL.md source pointer, sources.md created (methods-refresh T8).
